/**
 * Stub file — see individual route files below.
 * This documents all API routes in one place.
 *
 * GET  /api/dashboard        → DashboardData aggregate
 * POST /api/emissions        → Create emission calculation
 * GET  /api/emissions        → List calculations for period
 * POST /api/emissions/[id]/approve → Approve a calculation
 * POST /api/suppliers/invite → Send supplier invite email
 * POST /api/webhooks/stripe  → Stripe webhook handler
 * POST /api/ai/ocr           → Claude OCR document extraction
 * GET  /api/reg-updates      → Regulation updates feed
 * POST /api/billing/checkout → Create Stripe checkout session
 * POST /api/billing/portal   → Create Stripe customer portal session
 * POST /api/scoring/recompute → Trigger compliance score recompute
 */
export {};
